# AGENTS.md

## 核心原则
- **信息来源**（权威性递减）：
  1. 国内外 AI 大厂（DeepSeek, 月之暗面, 智谱AI, OpenAI, Anthropic, Google DeepMind, Meta AI, Prime Agent 等）的**官方网站**（技术博客、产品文档、API参考）。
  2. 经同行评议的**学术论文**（arXiv, NeurIPS, ICML, ACL 等）。
  3. 国外 AI 大厂**工程师的个人技术博客**（需确认其身份）。
  > 除以上来源外，其他信息（如知乎、公众号、非官方个人博客）仅作为参考，**不得作为核心技术依据**。
- **目标读者**：具备基础编程能力，对 AI / Agent / RAG 领域有宏观认识，但对具体技术细节、实现原理、调优方法不熟悉的开发者。文章需**由浅入深**，在解释核心概念时附带类比或示意图，允许使用伪代码，但必须说明。
- **图片来源**：
  - **仅允许**从上述权威信息来源中获取（原图或清晰截图），**严禁**使用第三方来源或AI生成图片。
  - 图片必须存放在 `resources/<文章英文标题>/` 目录下（与当前文章相对路径一致），文件名使用英文小写+连字符（如 `attention-mechanism.png`）。
- **避免重复**：若 `./content/zh-cn/posts/` 中已有内容相同的文章，应优先提供内部链接而非重新撰写；若需补充新视角，须明确说明差异。
- **版本控制**：AI 完成所有写作任务后，**禁止**自动执行 `git add`、`git commit` 或 `git push` 等任何提交操作。所有提交必须由人类作者手动完成。
- 参考文献必须精确到参考内容的网页，而非一个大概的网址

## 博客格式规范
- **Front matter**位于文章开头，YAML 格式：
    - description：精简的介绍博客，禁止任何的废话
    - tags 字段由文章内容决定，必须贯彻全文，是文章的主线，不超过3个
    **Example**
        ```yaml
        ---
        title: "Agent Loop 架构解析：为什么主流 Agent 都选 ReAct？"
        description: "Agent Loop 是 Agent 的核心循环。从 ReAct、Plan-and-Execute、ReWOO 到 Reflexion，看懂常见架构，也理解 ReAct 为什么成为主流默认。"
        date: 2026-08-25
        draft: false
        tags: ["Agent", "Agent Loop"]
        translationKey: "Agent Loop"
        ---
        ```
- 格式排版
    
## 写作流程（分阶段）
### 阶段一：确定选题（人类主导）
- **人类**：提供博客主题、内部文档、代码片段、相关 URL 等素材。
- **AI**：
  1. 根据人类提供的线索，检索权威来源（官网、论文、工程师博客），汇总关键信息。
  2. 调用 `/gill-me` 命令，向人类提出 5～8 个问题，以明确：
    - 文章核心要解决什么具体问题？
    - 读者最可能遇到的痛点是什么？
    - 人类是否已有内部实践或数据？
    - 期望的文章深度（概念级 / 实现级 / 调优级）？
    - 期望包含的小章节
  3. 根据人类回复，整理出“选题确认单”，包括：主题、目标、引用来源列表、是否引用本地文章、预期字数与风格

### 阶段二：大纲生成（AI主导，人类确认）
- **AI**：
  1. 基于选题确认单，生成详细的 Markdown 大纲，包含：
    - 标题（候选 2~3 个）
    - 引言（背景与问题）
    - 核心概念解析（分节，每节标注引用来源）
    - 总结与展望
    - 参考文献
  2. 大纲中需注明每个部分的图片需求（来自哪个来源的哪张图）。
- **人类**：审阅大纲，调整章节顺序、增删内容，确认后进入下一阶段。

### 阶段三：内容撰写（AI主导，人类审阅）
- **AI**：
  1. 严格按照确认后的大纲撰写全文，确保：
    - 每部分都引用权威来源（在正文中用 `[1]` 或 `（作者, 年份）` 标注，并在文末统一列出参考文献）。
    - 代码示例完整可运行（标明语言、依赖、运行方式），允许使用伪代码，附有注释。
    - 图片直接嵌入文中（使用 `![描述](resources/.../file.png)`），并在图下方注明来源。
    - 术语首次出现时给出中英文对照和简要定义，优先检查专业术语表。
    - 自然引入内部链接（如 `[之前文章](/posts/.../)`），避免重复内容。
  2. 撰写时需注意：
    - 段落之间过渡自然，由浅入深，每个新概念先类比后深入。
    - 文末提供“扩展阅读”。
  3. 完成初稿后，自行检查：
    - 所有引用是否可访问（URL 有效）。
    - 图片路径是否正确，图片是否清晰。
    - 代码是否可复现（必要时提供测试用例）。
- **人类**：审阅初稿，提出修改意见（增删内容、调整结构、修正错误等）。

### 阶段四：修订与定稿（AI主导，人类确认）
- **AI**：
  1. 根据人类的反馈，逐条修改，生成第二稿。
  2. 再次通读全文，优化措辞、统一术语、消除冗余，确保流畅性和准确性。
  3. 生成最终稿（含完整的 front matter、参考文献、图片列表）。
  4. 提供一份“修改说明”，列出相对于初稿的所有改动（方便人类快速确认）。
- **人类**：审阅最终稿，确认无误后，手动执行 `git add`、`commit` 和 `push`。

